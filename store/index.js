const state = () => ({
    //
})

const getters = {
    user: state => state.auth.user,
    authenticated: state => state.authenticated
}

const actions = {
    //
}

const mutations = {
    //
}

export default {
    state,
    getters,
    actions,
    mutations
}
